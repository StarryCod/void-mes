name = "void-time"
main = "src/index.ts"
compatibility_date = "2024-12-30"

# Durable Objects binding
[[durable_objects.bindings]]
name = "CHAT_ROOM"
class_name = "ChatRoom"

[[durable_objects.bindings]]
name = "CALL_ROOM"
class_name = "CallRoom"

# Durable Objects migrations
[[migrations]]
tag = "v1"
new_classes = ["ChatRoom", "CallRoom"]

# Environment variables (set via wrangler secret or dashboard)
[vars]
DATABASE_URL = ""
APPWRITE_ENDPOINT = "https://fra.cloud.appwrite.io/v1"
APPWRITE_PROJECT_ID = "69923291001c25654226"

# Secrets (set via: wrangler secret put <NAME>)
# PUSHER_APP_ID, PUSHER_KEY, PUSHER_SECRET - not needed, using Durable Objects
